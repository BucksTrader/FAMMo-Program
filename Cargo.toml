[workspace]
members = [
    "programs/*"
]
resolver = "2"

[profile.release]
overflow-checks = false
lto = "fat"
codegen-units = 1
opt-level = "z"
strip = true
panic = "abort"
incremental = false
debug = false
debug-assertions = false